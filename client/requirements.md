## Packages
framer-motion | Page transitions and UI animations
date-fns | Date formatting for class schedules
react-day-picker | Date picker component (if needed by ui/calendar)
clsx | Class name utility (usually in base stack but good to be explicit)
tailwind-merge | Tailwind utility (usually in base stack)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
